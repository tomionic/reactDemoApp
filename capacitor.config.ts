import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'react.demo.app',
  appName: 'reactDemoApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
